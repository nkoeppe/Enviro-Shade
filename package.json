{
  "name": "enviro-shade",
  "version": "1.4.2",
  "description": "Browser extension for environment-aware visual indicators",
  "scripts": {
    "screenshots:chrome": "node scripts/screenshots-chrome.js",
    "screenshots:firefox": "node scripts/screenshots.js",
    "demo:start": "cd scripts/demo && ./start.sh",
    "demo:stop": "cd scripts/demo && ./stop.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "playwright": "^1.40.0",
    "playwright-webextext": "^0.0.4"
  },
  "dependencies": {
    "@playwright/test": "^1.55.1",
    "tslib": "^2.8.1"
  },
  "keywords": ["browser-extension", "environment", "development", "staging", "production"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/nkoeppe/Enviro-Shade.git"
  }
}
